node_modules/
.wrangler/
.dev.vars
# 本地私有部署配置（历史遗留：KV 绑定已移除，此文件不再被任何脚本读取，
# 保留忽略规则只是防止旧文件被误提交，可自行删除）
.deploy.local.json
# 旧版 scripts/with-config.mjs 生成的临时配置（脚本已删除，同属历史遗留）
.wrangler.local.toml
# scripts/push-gh.sh 的临时请求体/响应（中断时可能残留）
.push_*.json
*.log
.DS_Store
.secrets*.tmp
.shots/
# 本地回归工具（依赖外部 playwright-core，不随仓库分发）
e2e-prod.mjs
.e2e-*.png
# 本地 UI 调试截图
.ui-*.png
